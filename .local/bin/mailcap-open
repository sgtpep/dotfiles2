#!/bin/bash
set -eu
find /tmp/ -maxdepth 1 -mtime +1 -name 'mutt-*' -type f -delete
if [[ ${1##*/} == *.* ]]; then
  path=${1%/*}/mutt-${1##*/}
else
  path=$1.html
fi
mv -f "$1" "$path"
setsid xdg-open "$path" &> /dev/null &
