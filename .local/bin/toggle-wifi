#!/bin/bash
set -eu
! command -v rfkill > /dev/null || sudo rfkill "$1" wifi
if [[ $1 == unblock ]]; then
  echo -n 'Connecting...' >&2
  until ping -c 1 danil.mobi &> /dev/null; do
    sleep 1
  done
  echo >&2
fi
