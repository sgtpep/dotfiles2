#!/bin/bash
set -eu
if [[ $(< /etc/os-release) == *=raspbian* ]]; then
  command=chromium-browser
else
  command=${0##*/}
fi
set -- /usr/bin/"$command" "$@"
[[ -x $1 ]] || install-packages "$command" fonts-liberation2
exec "$@"
