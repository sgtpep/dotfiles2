#!/bin/bash
set -eu
if ! command -v ansible-pull > /dev/null; then
  sudo apt update
  sudo apt -y install ansible git
fi
exec ansible-pull -K -U https://github.com/sgtpep/dotfiles2.git .local/share/playbooks/desktop.yml
